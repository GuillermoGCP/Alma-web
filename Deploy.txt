Comandos para conectar al hosting (SSH): 
ssh almalactancia@hl1499.dinaserver.com -p 22


 (Ctrl+Shift+P) → “SFTP: Sync Local → Remote” 
 Ya sube el contenido de dist a remoto. En la config está activado sync para que actualice y no borre. ver: SFTP: config


 *Comprobar CORS (bash):
 curl -i -X OPTIONS https://api.almalactancia.org/health \
  -H "Origin: https://almalactancia.org" \
  -H "Access-Control-Request-Method: GET"


Reinicias la app:
Panel Dinahosting → Otras aplicaciones → tu app Node → Reiniciar, o

Por SSH (Passenger):
cd ~/www/api
mkdir -p tmp && touch tmp/restart.txt